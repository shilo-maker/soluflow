PORT=5002
NODE_ENV=production
CLIENT_URL=https://your-app-name.onrender.com

# Database - Render will provide a PostgreSQL database URL
# For production on Render, this will be automatically set
DATABASE_URL=

# For local development with PostgreSQL
DB_DIALECT=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=soluflow
DB_USER=postgres
DB_PASSWORD=postgres

# For local development with SQLite (comment out DATABASE_URL)
# DB_DIALECT=sqlite
# DB_STORAGE=./database.sqlite

# JWT
JWT_SECRET=your_random_secret_key_change_this_in_production
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d
