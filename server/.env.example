PORT=5002
NODE_ENV=production
CLIENT_URL=https://your-app-name.onrender.com

# Database - Render will provide a PostgreSQL database URL
# For production on Render, this will be automatically set
DATABASE_URL=

# For local development with PostgreSQL
DB_DIALECT=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=soluflow
DB_USER=postgres
DB_PASSWORD=postgres

# For local development with SQLite (comment out DATABASE_URL)
# DB_DIALECT=sqlite
# DB_STORAGE=./database.sqlite

# JWT
JWT_SECRET=your_random_secret_key_change_this_in_production
JWT_EXPIRES_IN=30d
JWT_REFRESH_EXPIRES_IN=7d

# ===================================
# EMAIL CONFIGURATION
# ===================================
# Choose ONE of the following email service options:

# Option 1: Resend (Recommended for production)
# Get API key from: https://resend.com
# EMAIL_SERVICE=resend
# RESEND_API_KEY=re_your_api_key_here
# EMAIL_FROM="SoluFlow" <noreply@yourdomain.com>

# Option 2: SendGrid
# Get API key from: https://sendgrid.com
# EMAIL_SERVICE=sendgrid
# SENDGRID_API_KEY=SG.your_api_key_here
# EMAIL_FROM="SoluFlow" <your-verified-email@gmail.com>

# Option 3: Gmail SMTP (for testing/small apps)
# Generate app password: https://myaccount.google.com/apppasswords
# EMAIL_SERVICE=smtp
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-16-character-app-password
# EMAIL_FROM="SoluFlow" <your-email@gmail.com>

# Option 4: Custom SMTP Server
# EMAIL_SERVICE=smtp
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-smtp-username
# SMTP_PASS=your-smtp-password
# EMAIL_FROM="SoluFlow" <noreply@yourdomain.com>

# Option 5: Ethereal Email (Development/Testing only - auto-generates account)
# No configuration needed - just don't set EMAIL_SERVICE or use:
# EMAIL_SERVICE=smtp
# Emails will be captured and preview URLs shown in console

# Testing
TEST_EMAIL=your-test-email@example.com
